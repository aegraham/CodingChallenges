namespace CodingChallenges.RailFence;

public class RailFence
{
    public static string? Encode(int key, string? plainText)
    {
        var result = string.Empty;

        return result;
    }

    public static string? Decode(int key, string? encodedText)
    {
        var result = string.Empty;

        return result;
    }
}